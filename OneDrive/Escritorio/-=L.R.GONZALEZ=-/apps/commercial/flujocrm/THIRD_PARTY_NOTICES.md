# THIRD_PARTY_NOTICES

FlujoCRM is a proprietary commercial app. It depends on npm packages listed in
`package.json`, including Electron, Electron Builder, better-sqlite3 and
Chart.js.

Dependency source code is not copied into this source package. Generated
`node_modules` and `dist` artifacts are denied from source releases.
