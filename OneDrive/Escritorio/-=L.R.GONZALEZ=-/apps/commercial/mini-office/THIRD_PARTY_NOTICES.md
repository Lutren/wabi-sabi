# THIRD_PARTY_NOTICES

Mini Office is treated here as a proprietary commercial app candidate. It
declares Python dependencies in `pyproject.toml`, including Flask, requests
and Pillow, plus optional development/audio/video/social extras.

Dependency source code is not copied into this source package. Virtualenvs,
caches and generated reports are denied from source releases.
