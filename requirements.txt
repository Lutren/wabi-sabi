# MEDIOEVO Tools — Dependencias Python

# KDP Pipeline
pyyaml>=6.0
deepl>=1.17.0

# Orquestador Multi-IA (instala solo lo que vayas a usar)
openai>=1.0.0           # GPT-4o + DeepSeek + Grok (usan la misma API)
anthropic>=0.25.0       # Claude
google-generativeai>=0.5.0  # Gemini
